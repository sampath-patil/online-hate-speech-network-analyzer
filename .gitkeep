# This file exists only so that git tracks the (otherwise empty) models/ folder.
# Trained classifiers are saved here as .joblib files when you use the app's
# save function. Those artifacts are gitignored — see ../.gitignore
